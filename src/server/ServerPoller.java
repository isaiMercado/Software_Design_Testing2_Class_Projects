package server;

/**
 * Polls the server asynchronously every short time interval to retrieve the model
 */
public class ServerPoller {

    /**
     * Server proxy that is polled for model updates
     */
    private ServerProxy proxy;

    /**
     * Starts the asynchronous loop for polling from the server
     */
    public void initiatePolling() {

    }
}
